import { Page } from "~/components/page";

export default function DashboardVotersPage() {
  return (
    <Page title="Voters">
      <pre className="text-center">{`
TODO: Import Voters with CSV

If Pool has created a new VoteToken - button to transfer tokens to voters so they can vote`}</pre>
    </Page>
  );
}
