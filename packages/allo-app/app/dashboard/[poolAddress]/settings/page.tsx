import { Page } from "~/components/page";

export default function DashboardSettingsPage() {
  return (
    <Page title="Pool Settings">
      <pre className="text-center">{`
    TODO: Pool Form to update title, description, admins, tokens etc.
    
    (same as create pool) 
    `}</pre>
    </Page>
  );
}
